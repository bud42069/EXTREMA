[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["frontend", ".venv", "dist", "build"]

[tool.ruff.lint]
select = ["E","F","I","UP","N","B"]

[tool.pytest.ini_options]
pythonpath = ["backend"]
addopts = "-q"
